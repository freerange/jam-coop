name: Install libvips package

runs:
  using: composite
  steps:
    - name: Update package index files
      shell: bash
      run: sudo apt-get update

    - name: Install libvips
      shell: bash
      run: sudo apt-get install -y libvips
