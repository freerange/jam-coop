name: CI
on: [push]

jobs:
  tests:
    name: Tests
    runs-on: ubuntu-latest

    env:
      RAILS_ENV: test
      PGHOST: localhost
      PGUSER: myapp
      PGPASSWORD: password

    services:
      postgres:
        image: postgres:15-alpine
        env:
          POSTGRES_USER: myapp
          POSTGRES_DB: myapp_test
          POSTGRES_PASSWORD: password
        ports: ["5432:5432"]

    steps:
      - name: Checkout code
        uses: actions/checkout@v7

      - name: Install libvips package
        uses: ./.github/actions/install-libvips

      - name: Install ffmpeg package
        uses: ./.github/actions/install-ffmpeg

      - name: Setup Ruby and install gems
        uses: ruby/setup-ruby@v1.321.0
        with:
          bundler-cache: true

      - name: Setup dummy application environment using dotenv
        run: cp .env.example .env.test

      - name: Setup test database
        run: |
          bin/rails db:setup

      - name: Run linters
        run: |
          bin/rails htmlbeautifier rubocop factory_bot:lint

      - name: Run tests
        run: bin/rails test

      - name: Install Node
        uses: actions/setup-node@v7.0.0
        with:
          node-version-file: .node-version
          cache: npm

      - name: Install Node dependencies
        run: npm ci

      - name: Identify playwright version
        id: identify-playwright-version
        run: echo "playwright_version=$(npm run --silent playwright:version)" >> "$GITHUB_OUTPUT"

      - name: Cache playwright dependencies
        uses: actions/cache@v6
        id: cache-playwright-deps
        with:
          path: ~/.cache/ms-playwright
          key: ${{ runner.os }}-playwright-v${{ steps.identify-playwright-version.outputs.playwright_version }}

      - name: Install playwright
        if: ${{ steps.cache-playwright-deps.outputs.cache-hit != 'true' }}
        run: npx playwright install --with-deps chromium

      - name: Run system tests
        run: bin/rails test:system

      - name: Upload system test screenshots & traces
        if: ${{ failure() }}
        uses: actions/upload-artifact@v7
        with:
          name: System test screenshots
          path: |
            - tmp/screenshots
            - tmp/playwright-traces

  notify:
    name: Notify
    runs-on: ubuntu-latest
    needs:
      - tests
    if: ${{ always() && github.ref == 'refs/heads/main' }}
    steps:
      - name: Slack notification
        uses: justinr636/github-action-slack-notify-build@v1
        with:
          channel_id: C09SW90K30E
          status: ${{ needs.tests.result == 'success' && 'SUCCESS' || 'FAILURE' }}
          color: ${{ needs.tests.result == 'success' && 'good' || 'danger' }}
        env:
          SLACK_BOT_TOKEN: ${{ secrets.SLACK_NOTIFICATIONS_BOT_TOKEN }}

  deploy:
    name: Deploy
    runs-on: ubuntu-latest
    needs:
      - tests
    if: ${{ github.ref == 'refs/heads/main' }}
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - name: Promote main -> production
        run: |
          git checkout production
          git merge main
          git push origin production
