<div class="jam-card stack">
  <div class="frame"><%= card_image(image) %></div>
  <h3 class="jam-card__title"><%= title %></h3>
  <% if local_assigns[:subtitle] %>
    <h4 class="jam-card__subtitle"><%= subtitle %></h4>
  <% end %>
</div>
